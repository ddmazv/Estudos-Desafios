export function mostraIdade(nome, idade) {
  return `a idade de ${nome} é ${idade}`;
}
export function mostraCidade(nomeMorador, cidade) {
  return `${nomeMorador} mora em ${cidade}`;
}
export function mostraHobby(hobby, nome) {
  return `o Hobby de ${nome} é ${hobby}`;
}
